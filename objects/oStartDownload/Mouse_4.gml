/// @description Insert description here
// You can write your code in this editor
if(global.download){
	file = http_get_file(global.URL, working_directory + "image.jpg");
	if(global.sound)
	audio_play_sound(Sound,1,false);
}